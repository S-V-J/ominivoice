# GPU-specific dependencies for voice-engine
# Install with: pip install -r requirements.txt -r requirements-gpu.txt

# CUDA-enabled PyTorch
torch==2.4.1 --index-url https://download.pytorch.org/whl/cu121
torchaudio==2.4.1 --index-url https://download.pytorch.org/whl/cu121

# CUDA-enabled ONNX Runtime
onnxruntime-gpu==1.19.2

# CTranslate2 with CUDA
ctranslate2==4.4.0

# Note: faster-whisper will use CUDA via CTranslate2 automatically
# when GPU is available and compute_type is set to float16

# JWT for test token validation
python-jose[cryptography]==3.3.0